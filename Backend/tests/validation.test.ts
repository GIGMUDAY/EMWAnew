import {describe,it,expect,vi,beforeAll} from 'vitest';process.env.NODE_ENV='test';process.env.DATABASE_URL='postgresql://unused:unused@localhost/unused';process.env.JWT_ACCESS_SECRET='test-secret-that-is-at-least-32-characters';const query=vi.fn();vi.mock('../src/db/index.js',()=>({pool:{query},tx:vi.fn()}));let request:any,app:any;beforeAll(async()=>{request=(await import('supertest')).default;app=(await import('../src/app.js')).app},30_000);describe('HTTP validation and security',()=>{it('has a health check',async()=>{const r=await request(app).get('/health');expect(r.status).toBe(200);expect(r.body.success).toBe(true)});it('rejects malformed expert applications',async()=>{const r=await request(app).post('/api/v1/public/expert-applications').send({email:'bad'});expect(r.status).toBe(400);expect(r.body.error.code).toBe('VALIDATION_ERROR')});it('rejects unauthenticated protected requests',async()=>{const r=await request(app).get('/api/v1/admin/dashboard');expect(r.status).toBe(401)});it('returns generic invalid login errors',async()=>{query.mockResolvedValueOnce({rows:[]});const r=await request(app).post('/api/v1/auth/login').send({email:'nobody@example.com',password:'wrong'});expect(r.status).toBe(401);expect(r.body.error.message).toBe('Invalid email or password')})});
