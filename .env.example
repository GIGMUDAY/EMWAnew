# Optional feed used by the rotating Global Media Voices section.
# Expected response: an array, or { "data": [...] }, where every item contains
# quote, author, role, and an optional sourceUrl.
VITE_GLOBAL_VOICES_API_URL=

# Backend API used by public forms such as newsletter subscriptions.
VITE_API_URL=http://localhost:4000/api/v1
